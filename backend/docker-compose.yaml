services:
  evolution_api:
    image: atendai/evolution-api:latest
    container_name: evolution_api
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - .env.evolution
    extra_hosts:
      - "host.docker.internal:host-gateway"